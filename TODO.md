# Voice Bubble - 功能待办

> 记录待开发功能的调研结论和实施计划。每个功能包含背景、方案、优先级。

---

## 智能文字后处理

**背景**：当前 `TextProcessor.swift` 只做硬编码语气词删除 + 字面字符串替换，不够智能。需要在不明显增加延迟的前提下提升后处理质量。

**结论**：ASR 转写本身耗时 0.5-2s，后处理加 100ms 以内用户完全无感。采用混合分层架构。

### Layer 1：规则层（增强现有逻辑）

- [ ] 扩充语气词列表（"那个"、"就是"、"然后"、"反正"、"其实吧"等高频口语词）
- [ ] 语气词上下文判断——"那个"作指示代词时保留，作语气词时删除
- [ ] 清理更多标点异常模式

> 延迟 <1ms | 内存：无额外 | 难度：简单

### Layer 2：ITN 格式化层（新增）

将"说出来的"转成"写出来的"格式。

- [ ] 中文数字 → 阿拉伯数字（"一千二百三十四" → "1234"）
- [ ] 日期格式化（"三月二十九号" → "3月29日"）
- [ ] 百分比（"百分之十五" → "15%"）
- [ ] 金额（"两百块钱" → "200元"）
- [ ] 时间（"下午三点半" → "下午3:30"）
- [ ] 电话号码/序列数字（"零二一" → "021"）

**数学表达式子模块（Math ITN）**：

简单级（规则直转）：
- [ ] 基本运算符："加/减/乘以/除以/等于" → "+/-/×/÷/="
- [ ] 比较符："大于/小于/大于等于/小于等于/不等于" → ">/</≥/≤/≠"
- [ ] 幂次："X的平方/立方/N次方" → "X²/X³/Xⁿ"
- [ ] 分数："N分之M" → "M/N"（注意倒序）
- [ ] 根号："根号X" → "√X"

复杂级（需 LLM 辅助）：
- [ ] 嵌套括号："x的平方加二x加一的全部除以x减一" → "(x²+2x+1)/(x-1)"
- [ ] 根号作用域："根号下x的平方加y的平方" → "√(x²+y²)"
- [ ] 积分/求导等高级表达式

检测逻辑：句中出现"加/减/乘/除/等于/平方/根号/次方/分之/积分/求导"等关键词时进入数学 ITN 分支。

> 延迟 <10ms（规则部分）| 内存 ~几MB | 难度：中等

### Layer 3：LLM 润色层（新增，按需触发）

用小型本地 LLM（Qwen3-0.5B 4bit，~0.5GB 内存）做口语→书面转换。

- [ ] 集成 MLX 推理 Qwen3-0.5B
- [ ] 设计触发条件（句长>20字 / 含口语标记词 / 缺标点 / 用户开关）
- [ ] 设计 prompt（简洁书面化，不改意思，不添加内容）
- [ ] 数学复杂表达式走 LLM 分支
- [ ] 内存不足时自动跳过此层

触发条件（满足任一）：
- 句子长度 > 20 字
- 包含口语标记词："就是"、"然后"、"反正"、"其实吧"
- 整句缺少逗号/句号
- 检测到复杂数学表达式
- 用户手动开启"润色模式"

> 延迟 50-100ms | 内存 ~0.5-1GB | 难度：较复杂

### 实施优先级

```
1. Layer 2 基础 ITN（数字/日期/百分比）    ← 投入产出比最高
2. Layer 2 数学 ITN 简单级（加减乘除、平方）← 紧随其后
3. Layer 1 增强（上下文感知语气词）         ← 简单但需要测试
4. Layer 3 LLM 润色                        ← 锦上添花
5. Layer 2 数学 ITN 复杂级（需 Layer 3 支撑）
```

### 技术选型备忘

| 组件 | 候选方案 | 备注 |
|------|---------|------|
| 中文 ITN | Swift 原生实现 / NeMo text processing (Python) | 中文模式有限，Swift 自写可控性更好 |
| 数学 ITN 规则 | Swift 正则 + 映射表 | 简单级足够 |
| 小 LLM | Qwen3-0.5B / Qwen3-1.5B (MLX 4bit) | 0.5B 在 Apple Silicon 上可达 525 tok/s |
| LLM 推理框架 | MLX (mlx-swift) | 项目已在用 MLX 跑 ASR |

---

*（新功能待办追加在下方）*
